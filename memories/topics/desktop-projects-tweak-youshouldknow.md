---
name: desktop-projects-tweak-youshouldknow
description: 桌面两个项目 tweak 和 youshouldknow 的位置与 GitHub 同步关系
metadata:
  node_type: memory
  type: project
  originSessionId: sess_9523491b-83bd-433d-81a2-4d5fdb39af2b
---

桌面三个文件夹中 KYE 排除，有效项目仅 tweak（tweakbyjie）和 youshouldknow，已完成体检、工程化补齐、联动完善、内容加厚、gh-pages 发布与跨仓库 Coverage 审计修复，提交信息使用中文；当前与远端完全同步（0 0，working tree clean）。
- 本地路径：`C:/Users/VOS-User/Desktop/tweak` 和 `C:/Users/VOS-User/Desktop/youshouldknow`
- 远端：`https://github.com/3304711297/tweakbyjie.git` 和 `https://github.com/3304711297/youshouldknow.git`，分支 main 跟踪 origin/main
- 入口：`.\tweakbyjie.cmd`（双运行时启动器，pwsh 优先回退 5.1）或 `powershell -ExecutionPolicy Bypass -File .\tweakbyjie.ps1`（需整仓下载，模块内用 $script:RepoRoot 锚点）
- youshouldknow 为 13 个中文分类的知识库（内容在 `docs/` 下，知识层与执行层联动），tweak 为 PowerShell 工具集（Loader + Modules/）+ ultimate-performance.pow
- 最新同步点（2026-08-21 完善批次）：tweak `53b54ac`（Part 5 拆分为 Modules/Defender.ps1 + Backup.Defender.ps1，约 95 个策略值快照闭环、菜单 5→2 按快照恢复；此前 b639664 修 $PSScriptRoot 回归/rebootRequired 抹除 bug、release.yml、CI 加固），youshouldknow `770f203`（同步 Part 5 描述；此前 dc5fab4 修 nav 标题/删 11 个 stub/lychee fail:true/Coverage 对齐/修真实死链）。两项目均 0 0 clean，Actions 全绿。注意：用户本机已应用过 Part 5 全部策略值（快照演练时 95 值全部 Present）。
- 第三批（2026-08-21）：tweak `47c7cba` 新增 `tweakbyjie.cmd` 双运行时启动器（pwsh 优先回退 5.1，ASCII 注释防 cmd 代码页问题），test.yml 拆为 pester(pwsh) + smoke-windows-powershell(5.1) 双任务。用户本机已装 PowerShell 7.6.5（winget 用户级，WindowsApps 别名，与内置 5.1 并存）；pwsh 下交叉验证全过，脚本依赖的计划任务/DISM/MMAgent/Appx cmdlet 在 pwsh 兼容层可用，实测 `0xFFFFFFFF = -1 (Int32)` 在 7.x 同样成立。
- 2026-08-21 审计发现并已修复：tweak 函数体内 $PSScriptRoot 解析到 Modules/ 目录（菜单7 找不到 .pow）→ 引入 `$script:RepoRoot`；Menu.ps1 Part8 回滚清 rebootRequired → 已删。youshouldknow 站点侧边栏曾显示字面 "xxx/index.md" → 改用 navigation.indexes。死链检查启用后确认的真实死链：DLSSTweaks 迁移至 emoose/、DISM 文档改 s14 地址、微软 Spectre FAQ 已下架。
- 最新同步点（2026-08-21 第四批）：tweak `1dbfa87` + tag **v0.1.0**（版本号体系：`$script:TweakVersion`、菜单横幅显示、CHANGELOG.md；Release 工作流已端到端验证，附件为整仓 ZIP+SHA256SUMS；此前 e9f7c31 往返测试），youshouldknow `b42fefd`（内容迁入 docs/ 标准布局 + pymdownx 代码高亮 + git-revision-date-localized 真实修订日期）。两项目均 0 0 clean，Actions 全绿。
- **ysk 布局契约变更**：内容现位于 `docs/` 下（13 分类 = docs/分类名）；根 README 只是项目简介；本地直接 `mkdocs serve/build` 可用（prepare-docs.sh 已删除）；Coverage manifest/mapping/reference 路径带 `docs/` 前缀，tweak 审计工具已同步。新增分类仍需同步 mkdocs.yml nav。注意：迁移/新文件未提交时修订日期插件会因无 git 历史在 --strict 下报警。
- 剩余完善空间盘点（2026-08-21）已全部执行完毕：v0.2.0 已发布、计数器统一/Invoke-BcdEdit 报错捕获/Y-N 统一/ysk footer+tracking+依赖锁定均落地（见下方里程碑行与 v0.2.0 里程碑条目）。**当前唯一确认的下一步（用户已回复"继续吧"）**：拆分 Menu.ps1 剩余六个内联 Part（Part 2/3/4/6/7/11）——注意此前「11 个功能模块全部为独立函数」的说法不准确，实际已迁出为独立函数的仅 Part 1/5/8/9/10 共 5 个，509 行的 Menu.ps1 主要就是这六个内联块加调度链。明确不做：内容扩写与新栏目（用户暂缓，但项目会持续加新内容）、执行参考的 :NNN 历史基线快照保留（头部已有边界声明）。工程侧已收尾：非交互入口+会话日志（`4d64e48`）、恢复语义统一（`10f442f`）、往返测试+CodeCoverage（e9f7c31）、版本号体系+首个 Release（v0.1.0）、模块化全部完成（`94aff84`）。**CI/发布契约（c529223/1fe92ea）**：lint/test/coverage 三工作流已合并为单一 ci.yml（lint、test、smoke-windows-powershell、coverage-audit 四任务），main 推送四项全绿后 release 任务自动发布——版本号=最新 v* tag 补丁位+1，打包前注入包内脚本 `$script:TweakVersion`（去 v 前缀），附 ZIP+SHA256SUMS；按 tag 的 release.yml 已删除。已验证 v0.1.1/v0.1.2/v0.1.3 三次自动发布。发布说明格式（用户指定，cbd5e7d）：**不写"CI 全绿后自动构建"类来源描述**，改为跳转链接——"变更内容：[LAST → V](compare 链接) · [CHANGELOG](链接)"加版本同步行；compare 链接指上一 tag 到当前版本的提交差异。注意：Menu.ps1 内联 Part 的 `$ok++` 是函数局部计数，Common.ps1 辅助函数用 `$script:ok`，两套计数并存，属已知轻微不一致。
- Coverage 审计契约：manifest 位于 youshouldknow，mapping/reference 从 youshouldknow/main 读取，`YOUSEHOULDKNOW-COVERAGE-CHECK.md` 属于 tweak 本地仓库；每份资料不得出现 manifest 外 ID，三份资料 ID 并集必须覆盖 manifest 44 项。`tweak` coverage、test、lint 与 youshouldknow docs CI 均已 success。
- youshouldknow 发布状态：gh-pages 分支已存在，站点 `https://3304711297.github.io/youshouldknow/` 与分类页可访问；2026-08-21 起为标准 docs/ 布局，本地直接 `mkdocs build --strict` / `mkdocs serve` 可用（需先 `python -m pip install -r requirements-docs.txt`）
- 非阻塞注意点：本机 Pester 已是 6.1.0（与 CI 一致），本地 `Invoke-Pester` 可直接跑通 14/14 用例（需 TWEAK_SKIP_ADMIN_CHECK=1）；MkDocs 本地栈与 CI 版本一致（mkdocs 1.6.1 / material 9.7.7 / redirects 1.2.3）；PSScriptAnalyzer 1.25.0 与 lychee 0.24.2 均已本地安装，推送前可完整复刻 lint/test/link-check。注意：本地跑 lychee 前先删掉 gitignore 的过期 `docs/` 复制目录（prepare-docs.sh 的残留快照会报已修复的死链假阳性）。
- 约定：后续检查只对比 tweak 和 youshouldknow，忽略 KYE；以 0 0 + working tree clean 为同步判定；新增分类需同步 README 与 mkdocs.yml nav；提交信息中文；不要用 reset/force push 覆盖 main

- 2026-08-22 三项目体检：发现并当日修复 tweak 自动发布版本号回退 bug——ci.yml 用 `git describe`（取"距 HEAD 最近的 tag"）计算下一版本，v0.1.6 与 v0.2.0 曾同指 e8fca23 导致 v0.2.0 后误发 v0.1.7~v0.1.9。修复（`f000eba`，自动发布 **v0.2.1**）：else 分支改 `git tag -l 'v*' | sort -V | tail -1`（版本序最高），tag 分支改 `grep -vxF "$VERSION" | tail -1`；CHANGELOG 补 0.1.7 小节（含版本号异常说明）并新增 0.2.1 条目；`$script:TweakVersion` 同步 0.2.1。已验证：CI 全绿、Release v0.2.1 为 Latest、compare 链接 v0.2.0...v0.2.1 正确、ZIP 包内版本已注入 0.2.1、下次自动发布将正确算出 v0.2.2。体检同时确认的非问题：openrouter 本地=远端产物 SHA256 一致、30/30 单测过、cron 无空提交；tweak 本地 Pester 25/25。
- 2026-08-24/25 最新同步与审计基线：tweak 本地/远端均为 `9093115`（tag `v0.2.8`），youshouldknow 本地/远端均为 `9da3e7d`；ysk 最近两次已推送提交为 `5ae61ef`（Front Matter 校验与页面元数据卡）及 `9da3e7d`（DDR5 与 DLSS 新内容）。本轮审计发现 tweak 的 `tools/knowledge.lock.json` 仍锁 `6503dc3...`，落后 ysk/main `9da3e7d...`，下一次正式 Coverage/Release 会被锁定最新门禁阻断；ysk 本地审计曾生成未跟踪 `tools/__pycache__/`，不应提交。
- 2026-08-25 安全加固七阶段全部完成并推送（细节见 [[project-security-audit-2026-08-25]]）：最新同步点 **tweak `52b87d0`**（Defender 隔离/lock+CMD 透传/退出码/备份校验/Coverage 函数契约/设计文档+发布串行化，共 6 提交）、**youshouldknow `fa6f8ad`**（菜单 6→7 与恢复说明/映射模块定位校准/部署互斥，共 3 提交）；lock=`fa6f8ad8aa59...` 与 ysk/main 精确一致；两仓 0 0 clean，tweak Pester 50/50（8 个测试文件）。**用户已拍板 lock 保持严格同步策略**：ysk 任何 main 提交（含 CI-only）都要求 tweak 提升 lock——正确顺序是先提交推送 ysk→取新 SHA→改 tweak lock→跑 Coverage→再推 tweak。**推送后 CI 实测**：tweak 首跑（`caca8dc`）coverage-audit 按设计被旧 lock 拦截、其后全绿；逐笔推送 6 提交触发自动发布连发 **v0.2.9~v0.2.14**（Latest=v0.2.14，指向 `52b87d0`，ZIP+SHA256SUMS 齐全）；ysk 3 次 docs 工作流全绿、Pages 已上线且线上电源计划页验证含"选项 7"。**教训：批量改动攒齐一次推送，避免每次全绿 main 推送都烧一个自动补丁版本。** 遗留项不止供应链四项——用户追问"只剩这四个了吗"后 grep 核对出完整清单（TweakVersion 停 0.2.1、主脚本 EAP=Continue、Service 误导消息、测试模式无备份、备份无机器绑定、31 处旧核查基线等），已记录于 [[project-security-audit-2026-08-25]]，修复顺序也在该文件。

**Why:** 用户多次要求检查“桌面两个项目（kye文件夹不是）和 GitHub 是否同步”，需明确有效项目边界、同步基线与发布状态；2026-08-20 追加 7f2720c 文档修正与 gh-pages API 激活后同步点与发布状态需更新。

- 2026-09-01 五仓库任务书 v4.2 批次后最新同步点：tweak 本地/远端均为 **`e5b0b6a`（tag `v0.2.21`，已发版，ZIP 内版本注入实测 0.2.21）**，youshouldknow 本地/远端均为 **`0f00fc0`**（front matter 三字段+覆盖矩阵+徽章/tags 批次，Pages 已部署）；tweak 的 knowledge.lock=`0f00fc09…` 与 ysk/main 精确一致，Coverage 审计 44/44/44 全过。两仓 0 0 clean。本批 tweak 提交链：c17ceaf(P1-10/11 预检灰掉+短语确认) → a97224a(P2-14 README) → ac25c73(lock) → a1ec89d(preflight.ps1 BOM 修复) → e5b0b6a(v0.2.21)。详见 [[five-repo-task-book-v42]]。
- 2026-09-02 全开源项目 README 现代化进化与 tweak 打包修复：
  1. 六大仓库（youshouldknow, tweakbyjie, make-bilibili-great-together, steamdb-chinese-plus, huggingface-chinese-plus, openrouter-chinese-plus）及个人主页 3304711297 全部完成 README 现代化重构（Badges、特性对比矩阵、极速上手、实测预览图、盘古之白排版）。
  2. ysk 升级后按严格契约自动提升 tweak 的 `knowledge.lock.json` 至 `b0bd3be`，所有仓库 CI 全绿。
  3. **tweak 发布包缺失 scripts 修复**：用户实测从 GitHub 下载 `tweakbyjie-v0.2.21.zip` 报错缺少 `scripts/preflight.ps1`。根因 `.github/workflows/ci.yml` 的 zip 打包命令漏掉了 `scripts` 目录；已修复 `ci.yml` 并向 main 提交推送 `f07e0fc`，同时本地重新打包含 `scripts/preflight.ps1` 的 zip 并用 `gh release upload --clobber` 重新覆盖 GitHub 上 `v0.2.21` 的 Release 资产。
  4. **DISM 进程锁定目录坑**：运行 tweak 菜单 10 查询虚拟化状态时，`Get-WindowsOptionalFeature` 会在后台拉起 `DismHost.exe` 继承运行目录为 CWD 并不立即退出，导致资源管理器删除运行目录报"文件夹正在使用"；需 kill 掉该 `DismHost.exe` 即可释放。
- 2026-09-03 新增三篇 AI 工具/软件技巧文章并双仓闭环：
  1. ysk 新增三篇实战文章：`AI浏览器自动化扩展禁用陷阱与数据恢复.md`、`hermes-agent-Windows部署与本地模型桥接实战.md`、`Windows命令行工具代理行为差异速查.md`。
  2. 修复外链并自动重新生成覆盖矩阵，front-matter-check 与 lychee link-check 均通过，CI 全绿（commit `2548e34`）。
  3. tweak 严格提升 `knowledge.lock.json` 至 `2548e34c17...`，Coverage 44/44/44 全绿（commit `0ff1080`），自动发版 CI 全绿。
- 2026-09-03 评估并吸收 Kiwi-Tweaks 2.0 精华（QoS DSCP 46 数据包优先）与双仓上游看门闭环：
  1. **`tweakbyjie` 落地 Part 12**：新增 `Modules/GameQos.ps1` 与 `Modules/Backup.GameQos.ps1`，为 CS2/Valorant/Apex/COD/LoL 等竞技网游注入 DSCP 46 加速转发标记；通过 `tests/GameQos.Tests.ps1` 单元测试验证（Pester 113 项全过，commit `1711920`）。
  2. **`youshouldknow` 落地深度科普**：新增 `docs/网络通信/Windows游戏网络QoS策略与DSCP原理.md`，剖析 DiffServ/802.1p 队列映射与 TCP CUBIC 原理，mkdocs strict 构建全绿（commit `e418d66`）。
  3. **Kiwi-Tweaks 上游看门 CI 上线**：两仓均配置 `.github/workflows/upstream-watch.yml` 与 `tools/check-upstream.py`，每周一定期巡检 `contactkiwitweaks-stack/Kiwi-Tweaks`，发现新提交时自动开启 Issue 提醒评估。
**How to apply:** 后续检查只对比 tweak 和 youshouldknow，忽略 KYE；路径固定为 Desktop 下，以 0 0 + working tree clean 为同步判定；新增分类需同步 README 与 mkdocs.yml nav；Pages 状态以 gh-pages 分支存在 + API source 配置为准；本地 Pester 版本不匹配不判为代码故障。

[[tweak-modularization-plan]] [[youshouldknow-modular-linkage]] [[youshouldknow-doc-details]] [[pow-file-traceability]]
- 事实核查第二批进度（2026-08-21）：A 组系统调优四篇（服务优化/VBS/Defender删除/优化原则，`20886f0`）+ B 组四篇主文（CPU调度/GPU管线/内存管理/存储NVMe，`e633ce7`）已完成并推送，全部对照 tweak 源码核验且声明属实（仅两处勘误：服务快照补 DelayedAutostart 说明、「源码行号」改函数定位）。**剩余 10 篇待续**：验机两篇（审核模式OOBE/激活指南）+ Windows网络栈优化原则（可 WebFetch 微软文档核验）、游戏性能验证流程（内部一致性）、社区源依赖五篇（键鼠TCP/Karing TUN/Steam/笔电控制中心/BIOS×2，以 ⚠️ 标注为主）。
- 事实核查第二批收尾（2026-08-21，`d623736`/`35e71cf`）：C 组验机两篇（审核模式经 Microsoft Learn 在线核验属实）+ 网络栈（核实 tweak 无网络栈执行项）+ D 组七篇（键鼠TCP/Karing/Steam/笔电/BIOS×2/验证流程，按证据等级 ✅/⚠️ 如实标注）+ 补上服务优化原则遗漏的小节。**至此全库内容页 100% 具备事实核查记录**。期间一次 link-check 失败为 runner 瞬时故障（lychee 步骤全 skipped 却 exit 22），重跑即绿。
- v0.2.0 里程碑（2026-08-21，ee3325f→e8fca23）：计数器统一 35 处、Invoke-BcdEdit 捕获报错文本、Y/N 统一为 Test-ConfirmChoice 五处；CHANGELOG 未发布转正 0.2.0，Loader 基线版本 0.2.0。**里程碑发布机制**：ci.yml 的 push 不过滤 refs（branches+tags 同用为 AND 语义，tag 推送不会触发——已踩坑两次：①AND 陷阱 ②tag 指向旧提交时按旧工作流文件触发），分支/tag 取舍由任务级 if 守卫；推 v* tag → 四项检查 → 按 tag 名发布。注意：**tag 必须指向包含最新 ci.yml 的提交**。ysk 同步小项：navigation.footer/tracking 已开，requirements-docs 锁定精确版本（mkdocs 1.6.1/material 9.7.7/redirects 1.2.3/rev-date 1.5.4）。用户明确：项目会持续加新内容，v0.2.0 是基线不是收官。
- 模块化最终收尾（2026-08-21，`202e77a`，自动发布 v0.1.6）：Part 2/3/4 → `Modules/Bcd.ps1`（Invoke-BcdAdvancedModule/Invoke-TestModeEnableModule/Invoke-TestModeDisableModule）、Part 6 → `Service.ps1`（Invoke-ServiceModule）、Part 7 → `Power.ps1`（Invoke-PowerModule）、Part 11 → `Mpo.ps1`（Invoke-MpoModule）。**Menu.ps1 现在 104 行纯调度链，11 个功能模块全部为独立函数**（此前 94aff84 的"全部独立"表述不实，已在本批真正达成并勘误）。Loader 模块清单 16 个文件。踩坑记录：按行号切分函数体时 Mpo 吞了分发链收尾两行、Menu 缺 else 开启行（全局括号凑平导致 parse 竟通过、运行时才炸）——修复用"定位特征行截断+补函数闭合"的确定性方法；验证脚本计数调用时须排除注释行（用 `^\s*Invoke-X\s*$` 匹配真实调用）。
- 启动配置文档扩充（2026-08-21，`f5a14f0`）：应用户提供的社区 BCD 资料收录四类知识项——HPET 辨析（禁设备不适合，应走 BOOT-001 调用策略，附 B 站文章 b23.tv/cOH0RxB 链接，lychee 实测可达）、内核调试反作弊风险警告（⚠️ 社区共识）、"脚本未采纳的 BCD 项"表（loadoptions 单独/三合一组合、nx OptIn、bootux disabled，均明确无编号不计入执行覆盖）；tweak 脚本零改动。用户决策模式：知识库可收录社区做法但须标注采纳边界与证据等级。
- DDR5 速查页图片转文字（2026-08-21，`977a72d`/`cd70bf6`）：内存超频页三张截图的内容此前已完全文字化于正文表格，图片属冗余已移除（含 images/ 目录约 3MB）；AM5 步骤中依赖截图箭头的两处表述改写为自洽描述，VDDQ「图中标注」改为「社区原资料标注」，分类目录描述同步。该页现为纯文字可搜索页面。
- 新增 M-die 超频文章（2026-08-21，`9473291`）：docs/内存超频/i7-12800HX海力士M-die超频参数.md——i7-12800HX + 海力士新 M-die 2×12G 平台两组实机作业（6400 基准+38/42/55 收紧变体、6200c38 稳妥版），原始截图保留于 images/04/05；换算自洽性已核（tRFCpb≈130ns/tRFC2≈160ns/tRFC≈258~295ns 符合 M-die 特性）；用户提供者澄清已写入阅读说明（tRCD/tRP 同值、变体列仅三行变化）。内存超频分类现为两篇文章。
- 新增英睿达超频文章（2026-08-21，`b4b13b7`）：docs/内存超频/i7-12800HX英睿达C8D-M8D1超频参数.md——i7-12800HX + 英睿达 2×16G（Micron C8D/M8D1，模组 CT16G56C46S5.M8D1）四组频率作业五列总表（原厂5400c38/6000基础/6000c38收紧/6200C42/6400），电压四组统一 1.10/1.10/1.80V；换算自洽已核（tRFCpb≈130-139ns、tRFC2≈160-170ns、tRFC 各组独立 824/940/824）。与海力士 M-die 文章互链；截图不入文（用户惯例）。内存超频分类现为三篇文章。
- 覆盖审计契约收紧（2026-08-21，ysk `c809bf9` / tweak `0392638`）：外部 AI 复审证实 Compare-IdSet 丢弃 missing 差集（只有 extra 判失败、并集兜底），已改为逐份 missing+extra 均判失败并删并集兜底；严格审计暴露 27 个真实漏项（合并编号标题如 `MEMORY-001/002` + Coverage Check 未标 CORE/CPU/POWER/REGISTRY 编号），展开/补全后 44/44/44/44 全绿。详见 [[cross-repo-coverage-audit]]。
- 外部文章《win11优化拓展》评估与收录（2026-08-23，ysk `6663cc4`）：用户问这篇"设备管理器全禁流"游戏优化文是否写进项目，结论**不整篇照收、按辨析文形式收录**（用户拍板：HPET 不禁用、走优化脚本 BOOT-001，其余按建议改进后入 ysk）。已发布 `docs/系统调优与安全/设备管理器禁用类优化辨析.md`（同步 nav+分类索引）：逐条 ✅⚠️❌🔴 定级——GS Wavetable/摄像头禁用零帧率收益，UMBus/PIC 安慰剂（UMBus 有枚举异常案例），HPET 禁用与既有「HPET 辨析」冲突→导向 BOOT-001，Hyper-V/虚拟字样设备全禁层次错误→导向菜单10 `Invoke-VbsModule` 功能层操作，❻fTPM 禁用＋清除 TPM 明确反对（TPM 不在性能路径，清除销毁 Hello 凭据/证书且不可逆、失 Win11 资格；AMD fTPM 卡顿已由 AGESA V2 1.2.0.7 修复，正解是升 BIOS）。唯一吸收项：网卡环保节能（EEE）关闭，以⚠️需实测等级在文中引向《Windows网络栈优化原则》第五节（网络栈文本身未改动）。工程确认：审计只扫 manifest/mapping/reference 三份固定资料且 knowledge.lock 锁定版本→新增知识页不触契约、无需提锁；mkdocs --strict 本地过、CI build/link-check/deploy 全绿、线上页 200；tweak 零改动。同日二轮（ysk `67860f4`）：用户提供《禁用设备列表.txt》三档清单截图（基本/深度/ACPI 勾选，约二十项）继续并入同文新增第三节——判定比第一份更差：系统计时器与 ACPI 热区域为危险项（热区域是笔电风扇调速/过热降频数据源），NDIS 虚拟网络适配器枚举器对本机有直接杀伤（Karing TUN 即此类，已互链 Karing 文章），Microsoft 虚拟驱动器枚举器破坏 ISO 挂载，PCI 加密/解密控制器多为缺驱动的 TPM/PSP 或 MEI 须先按硬件 ID 查身份，Numeric Data Processor 两现暴露清单质量；事实核查补十行、分类索引描述改"两份社区设备禁用清单"，CI 全绿+线上已含新内容。随后按用户纠正提交 `9958e91`：NDIS Virtual Network Adapter Enumerator 改为⚠️按具体实现判断，明确现代代理工具常自带/安装独立 TUN/Wintun，Karing 自带 TUN 不等于依赖该系统枚举器；自带独立设备且无其他依赖时通常可保持禁用，无法确认时不要当优化项盲禁。删除文章面向公众不应出现的“本机”及个体化结论，修复 ACPI 表格重复列；mkdocs 严格构建、CI build/link-check/deploy 与线上文案验证全过。用户明确要求仅改文档，不擅自修改设备状态。
- 2026-08-24 本地已从 GitHub 快进同步：tweak `9bc665b` → `9093115`（含 tag `v0.2.8`，远端新增 11 个提交），youshouldknow `9958e91` → `1ece26a`（远端新增 2 个提交）；两仓库 `main...origin/main` 均为 `0 0`，工作区干净。同步使用 `pull --ff-only`，未使用 reset 或覆盖本地提交。
